import datetime
from datetime import timedelta

guest_house_ranking = {
    "name": "yelp_gust_house_ranking",
    "start_date": datetime(2021, 2, 8),
    "schedule_time": timedelta(days=1),
    "num_nodes": 1
}